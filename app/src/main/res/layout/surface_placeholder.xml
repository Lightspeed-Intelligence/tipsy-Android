<?xml version="1.0" encoding="utf-8"?>
<!--
  Surface 首帧占位层（W1 计划 §12.2）。

  ready 前盖在 RN root view 之上，ready 后单次淡出并移除。
  **不用固定延时猜 ready** —— 延时短了闪白屏、长了拖慢首帧，且真机与模拟器
  的合适值不同（iOS b2773e1 处理过同一问题）。判据见
  RNSurfaceFragment.scheduleFirstFrameReveal。

  背景色用 windowBackground：与壳的主题一致，避免占位层本身造成一次视觉跳变。
  这里刻意**不放 loading 指示器** —— 首帧目标是几十毫秒级，转圈一闪而过
  反而更像卡顿。真需要时应由 RN 侧的 skeleton 承担。

  ⚠️ `tools:ignore="Overdraw"` 是**必要的**，不是图省事（2026-08-10 CI 实测报错）：
  Overdraw 检测器假定「布局是某 Activity 的 contentView」，于是判定「主题已画过
  一次背景，这里再画一次是浪费」。但本布局**不是窗口根**，而是 RN root view 之上
  的覆盖层（见 RNSurfaceFragment.onCreateView），它必须自己不透明 —— 否则会直接
  透出下面尚未渲染的 RN 视图，占位层就失去意义了。lint 文档里明说这个检测器
  「用不精确的模式匹配推断布局归属，可能误判」，此处正属该误报。
  故**不能**改成去掉 background 或 @null 来「修」告警。
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/windowBackground"
    android:importantForAccessibility="no"
    tools:ignore="Overdraw" />
